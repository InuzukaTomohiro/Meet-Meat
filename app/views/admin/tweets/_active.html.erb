<%= link_to admin_tweet_path(tweet), method: :patch, remote: true, class: "text-dark" do %>
  <% if tweet.is_active? %>
		<div class="active_btn"><strong>有効</strong></div>
	<% else %>
		<div class="no_active_btn"><strong>無効</strong></div>
	<% end %>
<% end %>